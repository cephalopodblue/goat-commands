server {
    listen 80;
    server_name {{ host }};

    location /static {
        alias /home/heather/sites/{{ host }}/static;
    }

    location / {
        proxy_pass http://unix:/tmp/{{ host }}.socket;
        proxy_set_header Host $host;
    }
}

